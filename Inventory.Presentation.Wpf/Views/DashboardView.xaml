<UserControl x:Class="Inventory.Presentation.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory.Presentation.Wpf.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="#dde5d5">

    <UserControl.Resources>
        <Style x:Key="DashboardCardStyle" TargetType="materialDesign:Card" BasedOn="{StaticResource MaterialDesignOutlinedCard}">
            <Setter Property="Width" Value="220"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="Margin" Value="16"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="materialDesign:RippleAssist.Feedback">
                <Setter.Value>
                    <SolidColorBrush Color="#EAA221" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp8"/>
                    <Setter Property="BorderBrush" Value="#274b14"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardIconStyle" TargetType="materialDesign:PackIcon">
            <Setter Property="Width" Value="48"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#274b14"/>
        </Style>

        <Style x:Key="CardTitleStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignHeadline6TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,12,0,4"/>
            <Setter Property="Foreground" Value="#274b14"/>
        </Style>

        <Style x:Key="CardDescriptionStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignBody2TextBlock}">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#505e21"/>
            <Setter Property="Opacity" Value="0.7"/>
            <Setter Property="Margin" Value="16,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="48,32,48,16">
            <TextBlock Text="Welcome Back!" 
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                       Foreground="#274b14"/>
            <TextBlock Text="Select an option below to get started." 
                       Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                       Foreground="#738e37"
                       Opacity="0.8"/>
        </StackPanel>

        <ItemsControl Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <materialDesign:Card Style="{StaticResource DashboardCardStyle}">
                <materialDesign:Card.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToInventoryCommand}" />
                </materialDesign:Card.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="ViewDashboard" Style="{StaticResource CardIconStyle}"/>
                    <TextBlock Text="Manage Inventory" Style="{StaticResource CardTitleStyle}"/>
                    <TextBlock Text="View, search, and edit all products and their variants." Style="{StaticResource CardDescriptionStyle}"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Style="{StaticResource DashboardCardStyle}">
                <materialDesign:Card.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding AddNewProductCommand}" />
                </materialDesign:Card.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="PackageVariantPlus" Style="{StaticResource CardIconStyle}"/>
                    <TextBlock Text="Add New Product" Style="{StaticResource CardTitleStyle}"/>
                    <TextBlock Text="Create a new product entry with variants, stock, and details." Style="{StaticResource CardDescriptionStyle}"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Style="{StaticResource DashboardCardStyle}">
                <materialDesign:Card.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding NavigateToSettingsCommand}" />
                </materialDesign:Card.InputBindings>
                <StackPanel VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="Cog" Style="{StaticResource CardIconStyle}"/>
                    <TextBlock Text="Settings" Style="{StaticResource CardTitleStyle}"/>
                    <TextBlock Text="Configure application preferences and user settings." Style="{StaticResource CardDescriptionStyle}"/>
                </StackPanel>
            </materialDesign:Card>

        </ItemsControl>
    </Grid>
</UserControl>