<UserControl x:Class="Inventory.Presentation.Wpf.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Inventory.Presentation.Wpf.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#dde5d5">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style x:Key="FormHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignSubtitle1TextBlock}">
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="Text" Value="Create a New Category"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Text" Value="Edit Category"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SaveCategoryButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="#274b14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Content" Value="Save Category"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                    <Setter Property="Content" Value="Update Category"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>
    <Grid Margin="48,32,48,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Settings" Style="{StaticResource MaterialDesignHeadline4TextBlock}" Foreground="#274b14"/>
                <TextBlock Text="Manage application settings and data." Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Foreground="#738e37" Opacity="0.8"/>
            </StackPanel>
            <Button Grid.Column="1"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Command="{Binding NavigateToDashboardCommand}"
                    VerticalAlignment="Center"
                    Foreground="#274b14">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ArrowLeft" Margin="0,0,8,0"/>
                    <TextBlock Text="Back to Dashboard"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="1">
            <materialDesign:Card Padding="24" UniformCornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp2">
                <StackPanel>
                    <TextBlock Text="Manage Categories" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Foreground="#274b14" Margin="0,0,0,8"/>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,16">
                        <Button Command="{Binding ShowAddFormCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="#274b14"
                                Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="PlusBox" Margin="0,0,8,0"/>
                                <TextBlock Text="Add New Category"/>
                            </StackPanel>
                        </Button>
                        <Button Content="Edit Selected" 
                                Command="{Binding ShowEditFormCommand}" 
                                Style="{StaticResource MaterialDesignOutlinedButton}" 
                                Margin="8,0,0,0"/>
                        <Button Content="Delete Selected" 
                                Command="{Binding DeleteCommand}" 
                                Style="{StaticResource MaterialDesignOutlinedButton}" 
                                Foreground="{DynamicResource MaterialDesign.Brush.Error}"
                                Margin="8,0,0,0"/>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsFormVisible}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                    </StackPanel>

                    <Border Visibility="{Binding IsFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Background="#F1F8E9"
                            Padding="16" CornerRadius="4" Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Style="{StaticResource FormHeaderTextStyle}"/>

                            <TextBox materialDesign:HintAssist.Hint="Category name"
                                     Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                <Button Command="{Binding SaveCommand}"
                                        Style="{StaticResource SaveCategoryButtonStyle}"/>
                                <Button Content="Cancel" 
                                        Command="{Binding CancelCommand}" 
                                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                                        Margin="8,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Existing Categories" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Foreground="#505e21" Margin="0,0,0,8"/>
                    <ListBox ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding SelectedCategory}"
                             MaxHeight="400"
                             Style="{StaticResource MaterialDesignCardsListBox}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Margin="16,8"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>